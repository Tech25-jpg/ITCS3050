# GROUP MEMBERS - (Gunturi Harini Bindu,) (Nash, Christopher), (Teah, Kevin)
# PROBLEM SET 1 - ALL IN
def all_in(a, b):
    for i in a:
        if i not in b:
            return False
    return True

lst_1 = [1, 2]
lst_2 = [1, 2, 3]
print(all_in(lst_1, lst_2))
print(all_in(lst_2, lst_1))


# PROBLEM SET 2 - CREATE A DICTIONARY
def create_dictionary(keys, values):
    result = {}
    for i in range(len(keys)):
        result[keys[i]] = values[i]

keys = ['peanut', 'dragon', 'star', 'pop', 'space']
values = ['butter', 'fly', 'fish', 'corn', 'ship']

# PROBLEM SET 3 - PRINT PAIR
def print_pair(dictionary, target):
    if target in dictionary:
        print("Key: {target}")
        print("Value: {dictionary{target}}")
    else:
        print("That pair does not exist!")

dictionary = {"spongebob": "squarepants", "patrick": "star", "squidward": "tentacles"}
print_pair(dictionary, "patrick")
print_pair(dictionary, "plankton")
print_pair(dictionary, "spongebob")